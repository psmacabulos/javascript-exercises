const getTheTitles = (arr) => {
    console.log(arr);
    let titles = arr.map((value, index) => value.title);
    console.log(titles);
    return titles
};

// Do not edit below this line
module.exports = getTheTitles;
